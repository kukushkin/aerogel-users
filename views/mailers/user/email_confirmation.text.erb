Hello, <%= user_email.user.full_name %>!

To confirm your email address, copy and paste the following URL into your browser's address bar and press Enter.

http://<%= config.hostname! %>/user/confirm_email?email=<%= user_email.email %>&token=<%= user_email.confirmation_token %>
