<div class="page-header">
  <h1>User profile</h1>
</div>

<p>
    Full name: <%= current_user.full_name %>
</p>
<p>
    emails: <%= current_user.emails.map(&:email).join(', ') %>
</p>
<p>
    authentication methods: <%= current_user.authentications.map(&:provider).join(',') %>
</p>
